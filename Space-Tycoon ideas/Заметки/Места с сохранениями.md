1 ~~PawnDataController (каждая пешка) dynamicParameters (или нужные ключи)~~
2 ~~Transform пешки (PawnBrain / корень юнита) позиция, поворот (через vector + quaternion или два vector)~~
3 ~~PawnNavMesh если важен сейв «на середине хода»: isMoving, цель/прогресс; иначе можно не сохранять и после загрузки сбрасывать агент~~
4 ~~TurnManager IsPlayerTurn; по каждому триггеру из listOfTriggers — isActive (и при необходимости ссылка на триггер по имени/ID сцены)~~
5 ClickableItemsController статус каждого пункта mainTaskScenario / sideTaskScenario (TaskItemStatus); поле currentTaskScenarioIndex, если используешь
6 SimpleEnemyAI (опционально) только если сохраняешься во время вражеского хода: индексы сценария; иначе можно не трогать
8 ~~HandleInittingGlobalVars / глобальные параметры только если в рантайме меняешь то, что должно пережить загрузку (например IS_STEP_BY_STEP_KEY и т.п.)~~
